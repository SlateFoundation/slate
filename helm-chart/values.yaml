# Default values for deployment
replicaCount: 1

hab:
  composite:
    mysql:
      pkg_ident: 'core/mysql'

# these can be made up
  mysql:
    app_username: admin
    app_password: A3OoeeEYrn
    bind: '0.0.0.0'

cert_manager:
  annotations:
    cluster_issuer: letsencrypt-prod
